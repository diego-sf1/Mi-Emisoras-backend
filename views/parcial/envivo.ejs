<div class="container">
	<div class="row">
		<div class="col-md-4">
			<label for="catStations">Categorias</label>
			<select name="catStations" id="catStations" onchange="mre.llenarSelectStation(this,'station')">
				<option value="">--Seleccione una categoria</option>
				<% for(var valor in categorias){ %>
				<option value="<%= categorias[valor].URL %>"><%= categorias[valor].text %></option>
				<% } %>
			</select>
			<label for="station">Estaciones</label>
			<select name="stations" id="station">
				<option value="">--Seleccione --</option>
				
			</select>
		</div>
	</div>
	<div class="row" id="locals">
		
	</div>
</div>

<script>

	
</script>